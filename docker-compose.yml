version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - hgo_data:/app
    environment:
      - GO_ENV=development
    restart: unless-stopped

volumes:
  hgo_data:
